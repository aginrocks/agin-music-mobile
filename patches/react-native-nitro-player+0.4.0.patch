diff --git a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/download/DownloadWorker.kt b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/download/DownloadWorker.kt
index 8691515..0637708 100644
--- a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/download/DownloadWorker.kt
+++ b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/download/DownloadWorker.kt
@@ -60,6 +60,11 @@ class DownloadWorker(
                 // Perform download
                 val localPath = downloadFile(downloadId, trackId, urlString, storageLocation)
 
+                // If worker was stopped (pause/cancel), don't report completion or error
+                if (isStopped) {
+                    return@withContext Result.success()
+                }
+
                 if (localPath != null) {
                     downloadManager.onComplete(downloadId, trackId, localPath)
                     Result.success()
@@ -75,6 +80,11 @@ class DownloadWorker(
                     Result.retry()
                 }
             } catch (e: Exception) {
+                // If worker was stopped (pause/cancel), don't report error or retry
+                if (isStopped) {
+                    return@withContext Result.success()
+                }
+
                 val errorReason =
                     when {
                         e.message?.contains("network", ignoreCase = true) == true -> DownloadErrorReason.NETWORK_ERROR
@@ -137,6 +147,11 @@ class DownloadWorker(
                 var lastProgressUpdate = System.currentTimeMillis()
 
                 while (inputStream.read(buffer).also { bytesRead = it } != -1) {
+                    // Check if worker was stopped (pause/cancel) - stop writing immediately
+                    if (isStopped) {
+                        return@withContext null
+                    }
+
                     outputStream.write(buffer, 0, bytesRead)
                     bytesDownloaded += bytesRead
 
diff --git a/node_modules/react-native-nitro-player/ios/equalizer/EqualizerCore.swift b/node_modules/react-native-nitro-player/ios/equalizer/EqualizerCore.swift
index bbfc1d3..b3de54a 100644
--- a/node_modules/react-native-nitro-player/ios/equalizer/EqualizerCore.swift
+++ b/node_modules/react-native-nitro-player/ios/equalizer/EqualizerCore.swift
@@ -128,7 +128,7 @@ class EqualizerCore {
         process: tapProcessCallback
       )
 
-      var tap: MTAudioProcessingTap?
+      var tap: Unmanaged<MTAudioProcessingTap>?
       let createStatus = MTAudioProcessingTapCreate(
         kCFAllocatorDefault,
         &callbacks,
@@ -136,7 +136,7 @@ class EqualizerCore {
         &tap
       )
 
-      guard createStatus == noErr, let audioTap = tap else {
+      guard createStatus == noErr, let audioTap = tap?.takeRetainedValue() else {
         print("❌ EqualizerCore: Failed to create audio processing tap, status: \(createStatus)")
         return
       }
diff --git a/node_modules/react-native-nitro-player/src/hooks/useDownloadedTracks.ts b/node_modules/react-native-nitro-player/src/hooks/useDownloadedTracks.ts
index 5c7f61f..ccd9dc6 100644
--- a/node_modules/react-native-nitro-player/src/hooks/useDownloadedTracks.ts
+++ b/node_modules/react-native-nitro-player/src/hooks/useDownloadedTracks.ts
@@ -64,25 +64,8 @@ export function useDownloadedTracks(): UseDownloadedTracksResult {
     isMounted.current = true
     refresh()
 
-    const unsubscribeComplete = downloadCallbackManager.subscribeToComplete(
-      () => {
-        refresh()
-      }
-    )
-
-    const unsubscribeStateChange =
-      downloadCallbackManager.subscribeToStateChange(
-        (_downloadId, _trackId, state) => {
-          if (state === 'completed') {
-            refresh()
-          }
-        }
-      )
-
     return () => {
       isMounted.current = false
-      unsubscribeComplete()
-      unsubscribeStateChange()
     }
   }, [refresh])
 
